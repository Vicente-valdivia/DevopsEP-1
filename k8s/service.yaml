apiVersion: v1
kind: Service
metadata:
  name: registrarcliente-service
spec:
  selector:
    app: registrarcliente
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
